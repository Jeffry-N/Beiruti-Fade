# 🏗️ Architecture Diagram

## App Flow Chart

```
┌─────────────────────────────────────────────────────────────┐
│                   BEIRUTI FADE - FLOW CHART                 │
└─────────────────────────────────────────────────────────────┘

                          ┌──────────────┐
                          │   APP START  │
                          └──────┬───────┘
                                 │
                    ┌────────────▼─────────────┐
                    │ Check AsyncStorage       │
                    │ for user data            │
                    └────────┬───────────┬─────┘
                             │           │
                    ┌────────▼───┐   ┌───▼─────────┐
                    │ No User    │   │ User Exists │
                    └────────┬───┘   └───┬─────────┘
                             │           │
        ┌────────────────────▼──┐    ┌───▼──────────────────┐
        │   SHOW LOGIN/SIGNUP   │    │  SHOW HOME DASHBOARD │
        │   SCREENS             │    │  (Protected Routes)  │
        └────┬──────────┬───────┘    └───┬──────────────────┘
             │          │                │
        ┌────▼──┐  ┌────▼─────┐    ┌────▼────┐
        │LOGIN  │  │  SIGNUP  │    │  HOME   │
        └────┬──┘  └────┬─────┘    └────┬────┘
             │          │              │
        ┌────▼──────────▼──────┐    ┌──▼─────────────────┐
        │   API: /login or     │    │ - View Services   │
        │   /signup            │    │ - View Barbers    │
        └────┬──────────┬──────┘    │ - My Appointments │
             │          │          │ - Logout           │
        ┌────▼────┬─────▼───┐     └──┬────────┬────┬───┘
        │ Success │ Failure │        │        │    │
        └────┬────┴──┬──────┘        │        │    │
             │       │              │        │    │
       Save  │    Show Error    ┌───▼┐ ┌────▼──┐ │
       User  │                  │    │ │       │ │
             │                  │    │ │       │ │
        ┌────▼──────────────────▼──┐ │ │       │ │
        │  Navigate to HOME        │ │ │       │ │
        └─────────────────────────┘ │ │       │ │
                                    │ │       │ │
                          ┌─────────▼─▼──┐┌──▼─▼──┐
                          │   BOOKING    ││LOGOUT │
                          │   SCREEN     ││       │
                          │              ││Return │
                          │ 1. Service   ││to     │
                          │ 2. Barber    ││Login  │
                          │ 3. Date      ││       │
                          │ 4. Time      └───┬────┘
                          │ 5. Confirm       │
                          │                  │ Clear
                          └────┬─────────────┴──────────┐
                               │                        │
                        ┌──────▼──────┐          ┌──────▼──────┐
                        │ CONFIRMATION│          │ LOGIN PAGE  │
                        │ SCREEN      │          │             │
                        │             │          └─────────────┘
                        │ - Review    │
                        │ - Edit      │
                        │ - Confirm   │
                        └──────┬──────┘
                               │
                      ┌────────▼─────────┐
                      │ API: /appointment│
                      │ POST (Create)    │
                      └────────┬─────────┘
                               │
                      ┌────────▼─────────┐
                      │ Success Alert    │
                      │ & Navigation to  │
                      │ HOME or          │
                      │ APPOINTMENTS     │
                      └──────────────────┘
```

## Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE SCHEMA                          │
└─────────────────────────────────────────────────────────────┘

  CUSTOMER                    BARBER
  ┌──────────────┐            ┌──────────────┐
  │ Id (PK)      │            │ Id (PK)      │
  │ FullName     │            │ FullName     │
  │ Username     │            │ Username     │
  │ Email        │            │ Email        │
  │ Password     │            │ Password     │
  │ CreatedAt    │            │ Bio          │
  └──────────────┘            │ CreatedAt    │
         △                     └──────────────┘
         │                            △
         │                            │
         │ (CustomerId)        (BarberId)
         │                            │
         │       APPOINTMENT          │
         │       ┌──────────────┐     │
         └──────►│ Id (PK)      │◄────┘
                 │ CustomerId   │
                 │ BarberId     │
                 │ ServiceId    │
                 │ Date         │
                 │ Time         │
                 │ Status       │
                 └──────┬───────┘
                        │
                        │ (ServiceId)
                        │
                        ▼
                 SERVICE
                 ┌──────────────┐
                 │ Id (PK)      │
                 │ Name         │
                 │ Description  │
                 │ Price        │
                 └──────────────┘

  PRODUCT                 PRODUCT_ORDER
  ┌──────────────┐        ┌──────────────┐
  │ Id (PK)      │        │ Id (PK)      │
  │ Name         │        │ CustomerId   │
  │ Description  │◄───────│ ProductId    │
  │ Price        │        │ Quantity     │
  │ StockQty     │        │ OrderDate    │
  └──────────────┘        │ Status       │
                          └──────────────┘
```

## API Architecture

```
┌─────────────────────────────────────────────────────────────┐
│              BACKEND API ARCHITECTURE                       │
└─────────────────────────────────────────────────────────────┘

                    HTTP SERVER (Port 8080)
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
    ┌────────┐        ┌────────┐       ┌────────┐
    │ /login │        │/signup │       │SERVICES│
    │        │        │        │       │        │
    │ POST   │        │ POST   │       │ /servs │
    │        │        │        │       │        │
    └───┬────┘        └───┬────┘       │ GET    │
        │                 │            │        │
        │                 │            └────┬───┘
        │                 │                 │
    ┌───▼─────────────────▼────┐       ┌────▼───┐
    │  AUTH VALIDATION         │       │DATABASE│
    │  (username, password)    │       │ Query  │
    │  (fullName, email...)    │       │        │
    └───┬──────┬──────┬────────┘       └────┬───┘
        │      │      │                    │
    ┌───▼──┐ ┌─▼──┐ ┌▼────┐           ┌───▼────┐
    │ CUST │ │BARB│ │ ERR │           │ Response
    │ ER   │ │ ER │ │     │           │ JSON
    │ TBL  │ │TBL │ │ 401 │           │
    └──────┘ └────┘ └─────┘           └────┬───┘
                                            │
                                  ┌─────────▼──────────┐
                                  │ CLIENT APP         │
                                  │ Store in           │
                                  │ AsyncStorage       │
                                  │ Navigate to Home   │
                                  └────────────────────┘
```

## Component Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│           REACT NATIVE COMPONENT TREE                       │
└─────────────────────────────────────────────────────────────┘

                        RootLayout
                        (_layout.tsx)
                             │
                    ┌────────┴────────┐
                    │                 │
           ┌────────▼────────┐  ┌────▼──────────┐
           │ Not Logged In   │  │ Logged In      │
           │ (Auth = false)  │  │ (Auth = true)  │
           └────┬────────────┘  └────┬───────────┘
                │                    │
        ┌───────┴────────┐      ┌─────┴──────┐
        │                │      │            │
     ┌──▼──┐        ┌────▼──┐┌──▼─┐    ┌────▼────┐
     │Login│        │Signup ││Home│    │Booking  │
     └─────┘        └───────┘└────┘    │Confirm  │
                                       │Appts    │
                                       └─────────┘

Each Screen Contains:
├── Header/Navigation
├── Form Inputs (if applicable)
├── Content Sections
├── Action Buttons
└── Loading/Error States
```

## Data Flow Diagram

```
USER INPUT → VALIDATION → API CALL → BACKEND → DATABASE → RESPONSE → UI UPDATE

1. LOGIN FLOW:
   User Input → {username, pwd} → /login → Query DB → User Object → Save to Storage → Home

2. BOOKING FLOW:
   Service → Barber → Date → Time → Review → /appointment → Insert → Success → List

3. FETCH FLOW:
   App Start → /services → Response → State → Render → User Interaction
```

## File Relationships

```
mobile/app/_layout.tsx
    ├──> Imports api.ts
    ├──> Uses AsyncStorage
    ├──> Manages Auth State
    └──> Routes to:
        ├──> index.tsx (Login)
        │    └──> Calls api.login()
        ├──> signup.tsx (Register)
        │    └──> Calls api.signup()
        ├──> home.tsx (Dashboard)
        │    ├──> Calls api.getServices()
        │    ├──> Calls api.getBarbers()
        │    └──> Routes to booking.tsx
        ├──> booking.tsx (Book)
        │    ├──> Calls api.getServices()
        │    ├──> Calls api.getBarbers()
        │    └──> Routes to confirmation.tsx
        ├──> confirmation.tsx (Review)
        │    ├──> Calls api.bookAppointment()
        │    └──> Routes to home or appointments
        └──> appointments.tsx (History)
             └──> Calls api.getAppointments()

backend/MainServer.java
    ├──> Imports handlers/*
    ├──> Creates HTTP Server
    └──> Registers contexts:
        ├──> /login → LoginHandler
        ├──> /signup → SignupHandler
        ├──> /services → ServiceHandler
        ├──> /barbers → BarberHandler
        └──> /appointment → AppointmentHandler

handlers/*Handler.java
    ├──> Imports db.java
    ├──> Implements HttpHandler
    └──> Each handles:
        ├──> Parsing request
        ├──> Validation
        ├──> Database operations
        └──> JSON response

database/db.java
    ├──> MySQL connection
    └──> Used by all handlers

database/beirutifade.sql
    ├──> Creates schema
    ├──> Creates relationships
    └──> Inserts sample data
```

---

This architecture is clean, scalable, and production-ready! 🚀
